---
title: "Backend (draft)"
description: "Draft placeholder. Prefer the API and Database tabs for current contracts."
---

<Warning>
  This tab is a **draft**. The organization of `apps/api` may evolve. Do not
  invest in deep Backend architecture pages yet.
</Warning>

Use as a stable reference:

- **[API](/api)** — HTTP auth, guides, OpenAPI
- **[Database](/database)** — schema and columns
- **[Local development](/getting-started/local-development)** — migrate and run the Go API

HTTP paths use `/api/.../profiles/{studioId}/...`. Product browser URLs use
`/orgs/:orgHandle/:handle/...`. See [AGENTS.md](https://github.com/fanivi/agata/blob/main/AGENTS.md)
in the monorepo for the dual path story.
